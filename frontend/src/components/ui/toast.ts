/**
 * Thin wrapper around sonner so callers stay decoupled.
 * Usage:
 *   import { toast, Toaster } from '@/components/ui/toast';
 *   <Toaster />
 *   toast.success('done');
 */
export { toast, Toaster } from 'sonner';
